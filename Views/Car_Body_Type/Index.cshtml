@model IEnumerable<The_Ride_You_Rent.Models.Car_Body_Type>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.BodyType)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.BodyType)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BodyID }) |
            @Html.ActionLink("Details", "Details", new { id=item.BodyID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BodyID })
        </td>
    </tr>
}

</table>
